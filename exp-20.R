# Set up the plot area
plot(x = NULL, y = NULL, # Empty data to create an empty plot
     xlim = c(0, 10),    # Specify x-axis limits
     ylim = c(0, 20),    # Specify y-axis limits
     xlab = "X-axis",    # Label for the x-axis
     ylab = "Y-axis",    # Label for the y-axis
     main = "Empty Plot with Axis Limits" # Title of the plot
)
